# OpenSpec change archive (migrated)

Historical CityQuest changes converted from `memory-bank-archive/archive/`.

- Format: OpenSpec change folders (`proposal.md`, `design.md`, `tasks.md`, delta `specs/`)
- Delta specs are **historical snapshots** already represented in `openspec/specs/` — do not re-archive/merge them
- Original Memory Bank files remain in `memory-bank-archive/` as the verbatim source
