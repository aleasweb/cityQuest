# Memory Bank Archive (read-only)

Historical artifacts from the former **cursor-memory-bank** workflow.

**Active workflow:** OpenSpec (`openspec/`, `/opsx-*` commands).

Do not treat files here as the source of truth for current work. Use `openspec/specs/` and `openspec/changes/` instead.

CQST task archives are also available in OpenSpec format under `openspec/changes/archive/` (proposal/design/tasks/delta specs). Original files here remain the verbatim source.
